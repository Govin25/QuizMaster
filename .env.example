# Environment Variables for Quainy Deployment

# Server Configuration
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production
GEMINI_API_KEY=your_gemini_api_key_here

# Environment (development, production)
NODE_ENV=development

# Client URL for CORS (comma-separated for multiple origins)
# IMPORTANT: Use HTTPS URLs in production
CLIENT_URL=http://localhost:5173,http://localhost:5174

# Optional: Override default log level (error, warn, info, debug)
LOG_LEVEL=info

# Production Security Notes:
# 1. Set NODE_ENV=production to enable HTTPS redirect and HSTS headers
# 2. Use strong JWT_SECRET (minimum 32 characters, random)
# 3. Set CLIENT_URL to your HTTPS frontend URL
# 4. Ensure your web server (Nginx/Apache) is configured for HTTPS
# 5. Obtain SSL/TLS certificate (Let's Encrypt recommended)
